node_modules
dist
.limcode
.claude

# legacy local-only jest configs (superseded by jest.backend.config.js)
jest.config.cjs
tsconfig.jest.json

# test artifacts
coverage
.jest-cache
.ts-jest
.cache

# lock files (project uses npm)
pnpm-lock.yaml

# logs / runtime artifacts
*.log
*.log.*
logs/
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
tmp-*.out.log
tmp-*.err.log

# packaged extension bundles
*.vsix

# env files (may contain API keys)
.env
.env.*
.env.local
.env.development
.env.production

# OS metadata
.DS_Store
Thumbs.db
desktop.ini

# editor swap / backup files
*.swp
*.swo
*~

# JetBrains IDE
.idea/

# TypeScript incremental build info
*.tsbuildinfo

# temp artifacts
.tmp/
diff_analyzer.py
graycode-settings.json
graycode-settings*.json
limcode-settings*.json

# local review notes
report.md
docs/

electron-app/release/
electron-app/*.tar.gz


# dev handover traces (removed from the repo)
.graycode
electron-app/PROGRESS.md

# portable instance data (dev runs write here)
electron-app/data/

# key/credential files (preventive: keep secrets out of the repo)
*.pem
*.key
*.p12
*.pfx
credentials*
secrets*
*.secret
